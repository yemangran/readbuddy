# Domain Docs

这是 single-context repo。

Engineering skills 探索 codebase 时，应先读取：

- 根目录 `CONTEXT.md`（如果存在）
- `docs/adr/` 中与当前工作相关的 ADR（如果存在）

领域概念命名应遵循 `CONTEXT.md` 中的词汇；如果输出与 ADR 冲突，应明确指出冲突。

约定的布局为：

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```
